# RALPH-WIGGUM.md - 자율적 장기 실행 작업 시스템

> "30일 동안 259개의 PR, 40,000줄의 코드를 자동 생성"
>
> — Boris Journey, Anthropic Claude Code 창시자

Ralph Wiggum 기법은 Stop Hook을 사용하여 Claude가 명시적 완료 조건이 충족될 때까지 자율적으로 작업을 계속하도록 합니다. Opus 4.5는 최대 4시간 49분 동안 자율적으로 구동할 수 있습니다.

## Core Concept

```yaml
philosophy:
  - "AI의 오류가 발생해도 전체 시스템이 계속 진화"
  - "Stop Hook으로 조기 종료 방지"
  - "명시적 완료 신호까지 작업 지속"
  - "무한 루프 방지를 위한 최대 반복 횟수 필수"
```

## Quick Start

### 1. 기본 Ralph Loop

```bash
# 기본 사용 (필수: --max-iterations)
/ralph-loop "테스트 커버리지를 80%까지 높여주세요" --max-iterations 20

# TDD 모드
/ralph-loop "사용자 인증 기능 구현" --tdd --run-tests "npm test" --max-iterations 15

# 검증 명령 포함
/ralph-loop "리팩토링" --verify "npm run lint && npm test" --max-iterations 30
```

### 2. 취소

```bash
# 루프 취소
/cancel-ralph

# 상태 저장 후 취소
/cancel-ralph --save-state
```

## Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       RALPH WIGGUM SYSTEM                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────┐     ┌─────────────┐     ┌─────────────┐               │
│  │  /ralph     │     │   CLAUDE    │     │   STOP      │               │
│  │   -loop     │────►│    CODE     │◄────│   HOOK      │               │
│  │  (Start)    │     │  (Engine)   │     │             │               │
│  └─────────────┘     └──────┬──────┘     │ ralph-loop  │               │
│                             │            │    .py      │               │
│                             │            └──────┬──────┘               │
│                             ▼                   │                      │
│  ┌─────────────────────────────────────────────┐│                      │
│  │              STATE MANAGEMENT                ││                      │
│  ├─────────────────────────────────────────────┤│                      │
│  │  .claude/ralph-status.json  (상태 추적)      ││                      │
│  │  .claude/todo.md            (작업 목록)      │◄─────────────────────┘
│  │  .claude/ralph-loop.log     (실행 로그)      │                      │
│  │  .claude/ralph-test-results.md (TDD 결과)   │                      │
│  └─────────────────────────────────────────────┘                      │
│                                                                         │
│  ┌─────────────────────────────────────────────┐                      │
│  │              COMPLETION SIGNALS              │                      │
│  ├─────────────────────────────────────────────┤                      │
│  │  RALPH_COMPLETE    - 명시적 완료             │                      │
│  │  [RALPH_DONE]      - 대체 형식               │                      │
│  │  RALPH_CANCELLED   - 취소                   │                      │
│  │  todo.md 비어있음   - 자동 완료              │                      │
│  └─────────────────────────────────────────────┘                      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

## Execution Flow

```
┌───────────────────────────────────────────────────────────────────────┐
│                          RALPH LOOP FLOW                               │
├───────────────────────────────────────────────────────────────────────┤
│                                                                        │
│   /ralph-loop 실행                                                     │
│        │                                                               │
│        ▼                                                               │
│   ┌─────────────┐                                                      │
│   │ Initialize  │  ralph-status.json 생성                              │
│   │   Status    │  iteration: 0, status: running                       │
│   └──────┬──────┘                                                      │
│          │                                                             │
│          ▼                                                             │
│   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐             │
│   │   Execute   │────►│   Verify    │────►│   Check     │             │
│   │    Task     │     │  (Optional) │     │ Completion  │             │
│   └─────────────┘     └─────────────┘     └──────┬──────┘             │
│                                                   │                    │
│                              ┌────────────────────┼────────────────┐   │
│                              ▼                    ▼                ▼   │
│                         RALPH_COMPLETE     MAX_ITERATIONS     CONTINUE │
│                              │                    │                │   │
│                              ▼                    ▼                │   │
│                         ┌────────┐          ┌────────┐             │   │
│                         │  DONE  │          │  STOP  │             │   │
│                         │ 🎉     │          │  ⚠️    │             │   │
│                         └────────┘          └────────┘             │   │
│                                                                    │   │
│                              ┌──────────────────────────────────────┘   │
│                              ▼                                         │
│                         iteration++                                    │
│                         Stop Hook → Continue                           │
│                              │                                         │
│                              └────────── LOOP ─────────────────────────┤
│                                                                        │
└───────────────────────────────────────────────────────────────────────┘
```

## TDD Mode

### Red → Green → Refactor Cycle

```
┌─────────────────────────────────────────────────────────────────┐
│                      TDD CYCLE IN RALPH LOOP                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   🔴 RED PHASE                                                   │
│   ┌────────────────────────────────────────┐                     │
│   │ 1. 실패하는 테스트 작성                 │                     │
│   │ 2. npm test → FAIL (expected)          │                     │
│   │ 3. 다음 단계 진행                      │                     │
│   └────────────────────────────────────────┘                     │
│                      ↓                                           │
│   🟢 GREEN PHASE                                                 │
│   ┌────────────────────────────────────────┐                     │
│   │ 1. 테스트 통과 최소 코드 작성           │                     │
│   │ 2. npm test → PASS                     │                     │
│   │ 3. verification-loop.py 자동 검증      │                     │
│   └────────────────────────────────────────┘                     │
│                      ↓                                           │
│   🔵 REFACTOR PHASE                                              │
│   ┌────────────────────────────────────────┐                     │
│   │ 1. 코드 개선 (중복 제거, 명명 개선)     │                     │
│   │ 2. npm test → PASS (확인)              │                     │
│   │ 3. 다음 기능 또는 RALPH_COMPLETE       │                     │
│   └────────────────────────────────────────┘                     │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### TDD Commands

```bash
# 기본 TDD 모드
/ralph-loop "User 모델 구현" --tdd --max-iterations 15

# 특정 테스트 파일 대상
/ralph-loop "API 엔드포인트 구현" --tdd --run-tests "npm test -- api.test.ts"

# E2E 테스트 포함
/ralph-loop "로그인 페이지 구현" --tdd \
  --run-tests "npm test && npx playwright test" \
  --max-iterations 20
```

## E2E Testing (Playwright Integration)

프런트엔드 작업 시 자동으로 E2E 테스트 프레임워크를 감지합니다:

```yaml
supported_frameworks:
  - playwright: playwright.config.ts 또는 @playwright/test
  - cypress: cypress.config.ts 또는 cypress 디렉토리
  - puppeteer: puppeteer 의존성

auto_detection:
  - UI 컴포넌트 변경 감지
  - 프런트엔드 키워드 감지 (page, button, form 등)
  - E2E 테스트 실행 제안
```

## Safety Features

### 무한 루프 방지

```yaml
required_limits:
  max_iterations: "필수 설정 (기본: 10)"
  max_consecutive_failures: 3
  timeout_minutes: 60

recommendations:
  small_task: "--max-iterations 10"
  medium_task: "--max-iterations 25"
  large_task: "--max-iterations 50"

warning:
  "무한 루프를 방지하려면 반드시 최대 반복 횟수와 완료 조건을 설정해야 합니다"
```

### 비용 관리

```yaml
cost_controls:
  track_iterations: true
  log_to_file: ".claude/ralph-loop.log"

recommendations:
  "Opus 4.5는 반복당 약 $0.05-0.20 비용 발생"
  "20회 반복 시 최대 $4 예상"
```

## State Management

### ralph-status.json

```json
{
  "iteration": 5,
  "maxIterations": 20,
  "status": "running",
  "currentTask": "API 엔드포인트 구현",
  "lastTestResult": "pass",
  "consecutiveFailures": 0,
  "startTime": "2024-01-15T10:00:00Z",
  "safeWord": "RALPH_COMPLETE",
  "tddMode": true,
  "verifyCommand": "npm test"
}
```

### 상태 값

| Status | Description |
|--------|-------------|
| `idle` | Ralph Loop 비활성화 |
| `running` | 실행 중 |
| `completed` | 완료 (완료 신호 감지) |
| `cancelled` | 사용자 취소 |
| `max_iterations_reached` | 최대 반복 도달 |

## Integration with Continuous Claude

Ralph Wiggum은 Continuous Claude 시스템과 완전히 통합됩니다:

```yaml
shared_components:
  handoff_file: ".claude/HANDOFF.md"
  todo_file: ".claude/todo.md"

stop_hooks:
  - continuous-loop.py  # HANDOFF.md 기반 루프
  - ralph-loop.py       # Ralph Wiggum 완료 감지
  - continuation-enforcer.py  # 미완료 작업 강제

coordination:
  - "두 시스템 동시 사용 가능"
  - "HANDOFF.md로 상태 공유"
  - "완료 신호 통합"
```

## Use Cases

### 1. 테스트 커버리지 증가

```bash
/ralph-loop "커버리지가 가장 낮은 파일부터 테스트 추가. \
목표: 전체 커버리지 80%" \
--tdd --run-tests "npm test -- --coverage" \
--max-iterations 30
```

**예상 결과**: 주말 동안 30개의 테스트 파일 추가

### 2. 대규모 마이그레이션

```bash
/ralph-loop "JavaScript 파일을 TypeScript로 마이그레이션. \
한 번에 하나의 파일만 처리." \
--verify "npm run typecheck" \
--max-iterations 50
```

**예상 결과**: CI 검증된 안전한 마이그레이션

### 3. API 개발 (TDD)

```bash
/ralph-loop "REST API 엔드포인트 구현. \
OpenAPI 스펙 기반으로 TDD 방식." \
--tdd --run-tests "npm test -- api/" \
--verify "npm run lint" \
--max-iterations 25
```

**예상 결과**: 테스트 커버리지 100%의 API

### 4. 프런트엔드 개발 (E2E)

```bash
/ralph-loop "로그인 페이지 구현 및 E2E 테스트" \
--tdd --run-tests "npm test && npx playwright test" \
--max-iterations 15
```

**예상 결과**: E2E 테스트로 검증된 UI

### 5. 기술 부채 해결

```bash
/ralph-loop "TODO 주석을 하나씩 해결하세요. \
각 TODO를 해결한 후 관련 테스트를 추가하세요." \
--max-iterations 40
```

**예상 결과**: 점진적인 기술 부채 감소

## Monitoring

### 실시간 상태 확인

```bash
# Ralph Loop 상태
cat .claude/ralph-status.json

# 실행 로그
tail -f .claude/ralph-loop.log

# TDD 테스트 결과
cat .claude/ralph-test-results.md
```

### 로그 위치

```
.claude/
├── ralph-status.json       # 현재 Ralph Loop 상태
├── ralph-loop.log          # 실행 로그
├── ralph-test-results.md   # TDD 테스트 결과 히스토리
├── todo.md                 # 작업 목록
└── HANDOFF.md              # Continuous Claude 인수인계
```

## Troubleshooting

| Issue | Solution |
|-------|----------|
| 무한 루프 | `--max-iterations` 반드시 설정 |
| 조기 종료 | `todo.md`에 미완료 작업 확인 |
| TDD 단계 인식 안됨 | 명시적 키워드 사용 (red, green, refactor) |
| E2E 프레임워크 미감지 | `package.json` 의존성 확인 |
| 비용 초과 | 반복 횟수 줄이고 작업 분할 |

## Best Practices

```yaml
do:
  - "항상 --max-iterations 설정"
  - "TDD 모드에서 작은 단위로 진행"
  - "검증 명령으로 품질 보장"
  - "todo.md로 진행 상황 추적"
  - "완료 시 RALPH_COMPLETE 명시적 출력"

dont:
  - "--max-iterations 없이 실행 ❌"
  - "너무 큰 작업을 한 번에 시도 ❌"
  - "테스트 없이 대규모 변경 ❌"
  - "완료 신호 없이 종료 기대 ❌"
```

## Related Commands

| Command | Description |
|---------|-------------|
| `/ralph-loop` | Ralph Loop 시작 |
| `/cancel-ralph` | Ralph Loop 취소 |
| `/continuous` | Continuous Claude 루프 |
| `/verify-app` | 앱 검증 (lint, test, build) |

## References

- [Ralph Wiggum Plugin - Anthropic](https://github.com/anthropics/claude-code/tree/main/plugins)
- [The Ralph Wiggum Technique](https://www.atcyrus.com/stories/ralph-wiggum-technique-claude-code-autonomous-loops)
- [Running Claude Code in a loop](https://anandchowdhary.com/blog/2025/running-claude-code-in-a-loop)
- [Boris Journey - 30일 259 PR 사례](https://www.linkedin.com/posts/wonjun-seo-서원준-6088a5379_github-anthropicsclaude-quickstarts)
- [Claude Quickstarts](https://github.com/anthropics/claude-quickstarts)

## Sources

이 문서는 다음 자료를 기반으로 작성되었습니다:

- [The Ralph Wiggum Technique - Cyrus](https://www.atcyrus.com/stories/ralph-wiggum-technique-claude-code-autonomous-loops)
- [Claude Ralph Plugin - Geeky Gadgets](https://www.geeky-gadgets.com/claude-ralph-plugin/)
- [Ralph Wiggum - Awesome Claude](https://awesomeclaude.ai/ralph-wiggum)
- [Anthropic Ralph Loop Guide - Kloudihub](https://news.kloudihub.com/anthropic-ralph-loop-claude-code-guide/)
